# Третье устройство: ASTERION PLUS 5.6K

Разбор от 08.09.2026.

## Что это за инвертор

С шильдика: `MPPT SOLAR INVERTER, ASTERION PLUS 5.6K`, 5600 ВА / 5600 Вт,
вход 48 В, выход 230 В 24,3 А, заряд от сети до 120 А (по умолчанию 30 А),
панели до 6000 Вт, MPPT 120–430 В, входной ток до 27 А.

**Это семейство Voltronic Axpert VM.** Совпадает и компоновка задней панели
(кольцо-подсветка, порт распределения тока, порт параллельного подключения,
сухой контакт), и характеристики: тот же 5,6 кВт на 48 В с MPPT 120–430 В
продаётся как `Voltronic Axpert VM IV 5.6KW`. До девяти штук в параллель,
режим Grid-tie с отдачей в сеть.

## Главное отличие от первых двух: другой язык

Первые два инвертора говорят **Modbus RTU** — различались только скорость,
адрес и карта регистров. Этот говорит **ASCII-протоколом Voltronic**, и это
принципиально другой разбор:

```
запрос:   QPIGS<CRC16-XMODEM><0x0D>       текстовая команда, два байта суммы, возврат каретки
ответ:    (значение значение значение …<CRC16><0x0D>
```

То есть не регистры по адресам, а строка полей, разделённых пробелами,
в фиксированном порядке.

Готовые кадры с посчитанными контрольными суммами (те же, что в `BLE-SNIFF.md`):

| Команда | Что спрашивает | Байты |
|---|---|---|
| `QPI` | версия протокола | `51 50 49 BE AC 0D` |
| `QID` | серийный номер | `51 49 44 D6 EA 0D` |
| `QMOD` | текущий режим работы | `51 4D 4F 44 49 C1 0D` |
| `QPIGS` | все текущие показания одной строкой | `51 50 49 47 53 B7 A9 0D` |
| `QPIRI` | номиналы и уставки | `51 50 49 52 49 F8 54 0D` |
| `QPIWS` | предупреждения и аварии | `51 50 49 57 53 B4 DA 0D` |

Ответ на `QPIGS` — около двадцати полей подряд: напряжение и частота сети,
напряжение и частота выхода, полная и активная мощность, загрузка, напряжение
шины, напряжение батареи, ток заряда, заряд батареи в процентах, температура,
ток и напряжение панелей, ток разряда и битовая строка состояния. Порядок
фиксированный, поэтому разбор — это разбиение по пробелам, а не поиск по адресу.

**Хорошая новость: заряд батареи в процентах здесь есть**, прямо в `QPIGS`.
Отдельных ухищрений, как на HS35, не потребуется.

## Разъёмы

По схеме из руководства:

| № | Разъём | Состояние |
|---|---|---|
| 12 | сухой контакт | не наш |
| 13 | **USB** | **свободен** |
| 14 | RS-232 | **занят батареей** |
| 15 | BMS: CAN, RS-485 и RS-232 (RJ45) | **занят батареей** |

**Оба последовательных порта отданы батарее, свободен только USB.** Тот же
расклад, что на HS35: путь через RS-232 с копеечной платой MAX3232 отпадает,
остаётся USB и, значит, ESP32-S3 в роли хоста.

### Отменённый вариант: RS-232 (порт 14)

Оставлено на случай, если порт когда-нибудь освободится: плата **MAX3232**
за 250 ₽ и **обычный ESP32**. Скорость у этого семейства обычно **2400 8N1**,
реже 9600 — подбирается за минуту командой `QPI`, самой безобидной из всех.

### Выбранный вариант: USB (порт 13)

Здесь **важное отличие от HS35**. У того за USB стоял мост CH340, то есть
обычный последовательный порт. У Voltronic за USB обычно **устройство HID**
(часто `VID 0665, PID 5161`), и команды ходят восьмибайтовыми HID-отчётами.
Драйвер нужен другой — не CH340, а HID-хост.

**Проверяется за тридцать секунд и решает объём работы:** воткнуть USB
инвертора в ноутбук с Linux и выполнить `lsusb`.

| Что покажет | Что это значит |
|---|---|
| `1a86:7523` | мост CH340, обычный последовательный порт. Подойдёт тот же драйвер, что пишется для HS35 — работы почти нет |
| `0665:5161` или подобное | устройство HID. Нужен третий драйвер: USB-HID-хост, команды восьмибайтовыми отчётами. В ESP-IDF для этого есть `usb_host_hid`, задача решаемая, но это отдельная работа |

Пока `lsusb` не сделан, оценить объём работы нельзя — разница между двумя
исходами примерно в неделю.

## Что меняется в прошивке

Разделение, сделанное для второго устройства, здесь проверяется на прочность —
и держит, но не полностью:

**Переиспользуется как есть:** весь показ, схема потоков, журнал событий, темы,
настройка сети, кнопка, ярлык, API, снапшот состояния.

**Пишется заново:** разбор ASCII вместо Modbus. `modbus.cpp` тут не подходит —
у Voltronic другой кадр и другая контрольная сумма (CRC16/XMODEM вместо
Modbus CRC16). Появится `voltronic.h/.cpp` рядом с `modbus.h/.cpp`, на том же
уровне.

**Профиль вместо карты регистров:** `profile_voltronic.cpp` разбирает поля
ответа по позициям. Интерфейс профиля прежний — «получи ответ, разложи
в снапшот», — так что цикл опроса и транспорт не меняются.

То есть слоёв по-прежнему три, просто на среднем появляется второй жилец.
Это ровно та развилка, ради которой разделение и делалось: **новый инвертор —
это два новых файла, а не новая прошивка**.
