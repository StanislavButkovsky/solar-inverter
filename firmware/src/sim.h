// Демо-данные: батареи рядом нет, а веб-интерфейс показать надо.
// Генерирует правдоподобную сборку 14S NMC. Включается явно (CLI «sim on»
// или кнопка в вебе) и всегда честно подписывается в интерфейсе, чтобы
// демонстрационные цифры нельзя было спутать с настоящими.
#pragma once
#include <Arduino.h>

void simStart();
void simSetEnabled(bool on);
bool simEnabled();
